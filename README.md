# FSC-vs-Behavioral-Cloning
 This repository evaluates the ability of the Finite State Controller (FSC) method to decode an agent's memory in a partially observable environment (POMDP), comparing it with standard Deep Learning methods.
